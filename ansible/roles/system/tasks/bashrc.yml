- name: Update bashrc to autostart neofetch
  ansible.builtin.lineinfile:
    dest: "{{ home }}/.bashrc"
    line: neofetch
    mode: "{{ docker_compose_directory_mask }}"
    owner: "{{ user }}"
    state: present
    insertafter: EOF
    create: true
